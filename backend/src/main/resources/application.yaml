server:
  port: ${PORT:8080}

spring:
  application:
    name: ASE251S4_T10-be
  data:
    mongodb:
      database: ASE251S4_T10
      uri: ${MONGODB_URI:mongodb+srv://yancarloshuacre_db_user:5XfG1D7hZ5MnziV1@cluster0.mfokbuv.mongodb.net/ASE251S4_T10?retryWrites=true&w=majority&appName=Cluster0}
  datasource:
    url: ${DATABASE_URL:jdbc:h2:mem:tse_db;DB_CLOSE_DELAY=-1;MODE=MSSQLServer}
    username: ${DATABASE_USERNAME:sa}
    password: ${DATABASE_PASSWORD:}
    driver-class-name: ${DATABASE_DRIVER:org.h2.Driver}
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: false

springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    # http://localhost:8080/swagger-ui.html